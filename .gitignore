# Local Environment Secrets
.env
.env.local

# Data Folder (Databases and Stats)
data/*.db
data/*.db-journal
data/*.json
# But keep the data directory itself if needed (though app.py creates it)
!data/.gitkeep

# Python caching & artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage

# IDEs & System files
.vscode/
.idea/
.gemini/
.DS_Store
Thumbs.db
