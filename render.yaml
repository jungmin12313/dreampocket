services:
  - type: web
    name: dreampocket
    plan: starter
    env: docker
    envVars:
      - key: FLASK_ENV
        value: production
    disk:
      name: scholarship-db
      mountPath: /opt/render/project/src/data
      sizeGB: 1
